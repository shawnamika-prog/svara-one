(() => {
  if (window.SvaraModal) return;
  let root = null, resolver = null, keyHandler = null, previewItem = null;
  function ensureRoot(){if(root)return root;root=document.createElement('div');root.className='svara-modal-root';root.hidden=true;root.innerHTML='<section class="svara-modal" role="dialog" aria-modal="true" aria-labelledby="svaraModalTitle"><div class="svara-modal-head"><div><p class="svara-modal-eyebrow">SVARAONE</p><h2 id="svaraModalTitle" class="svara-modal-title"></h2></div><button class="svara-modal-close" type="button" aria-label="Close">×</button></div><div class="svara-modal-body"></div><div class="svara-modal-foot"><button class="svara-modal-button" type="button" data-modal-cancel>Close</button><button class="svara-modal-button primary" type="button" data-modal-confirm>Rename</button></div></section>';document.body.appendChild(root);root.querySelector('.svara-modal-close').addEventListener('click',()=>finish(null));root.querySelector('[data-modal-cancel]').addEventListener('click',()=>finish(null));root.addEventListener('click',e=>{if(e.target===root)finish(null);});root.querySelector('[data-modal-confirm]').addEventListener('click',()=>finish(root.querySelector('.svara-modal-input')?.value??true));return root;}
  function finish(value){if(!root||root.hidden)return;const current=resolver;resolver=null;if(keyHandler)document.removeEventListener('keydown',keyHandler,true);keyHandler=null;root.classList.remove('is-open');setTimeout(()=>{if(root)root.hidden=true;},160);if(current)current(value);}
  function openRename(initialValue){const modal=ensureRoot();modal.querySelector('.svara-modal-title').textContent='Rename generation';modal.querySelector('.svara-modal-body').innerHTML='<label class="svara-modal-label" for="svaraRenameInput">Generation name</label><input id="svaraRenameInput" class="svara-modal-input" type="text" autocomplete="off" spellcheck="false"><p class="svara-modal-help">Choose a clear name for this audio asset. The file format will be preserved automatically.</p>';const confirm=modal.querySelector('[data-modal-confirm]');confirm.hidden=false;confirm.textContent='Rename';const input=modal.querySelector('#svaraRenameInput');input.value=String(initialValue??'');root.hidden=false;requestAnimationFrame(()=>root.classList.add('is-open'));keyHandler=e=>{if(e.key==='Escape'){e.preventDefault();finish(null);}else if(e.key==='Enter'&&document.activeElement===input){e.preventDefault();finish(input.value);}};document.addEventListener('keydown',keyHandler,true);setTimeout(()=>{input.focus();input.select();},30);return new Promise(resolve=>{resolver=resolve;});}
  function openPreview(mediaUrl,item={}){const modal=ensureRoot();modal.querySelector('.svara-modal-title').textContent='Preview';modal.querySelector('[data-modal-confirm]').hidden=true;const body=modal.querySelector('.svara-modal-body');body.innerHTML='';const filename=String(item.filename||'');const type=String(item.mediaType||item.type||'').toLowerCase();const video=type==='video'||/\.(mp4|webm|mov|m4v)$/i.test(filename);const media=document.createElement(video?'video':'audio');media.className='svara-modal-media';media.controls=true;media.preload='metadata';media.playsInline=true;media.src=mediaUrl;media.setAttribute('aria-label',`Preview of ${filename||'generation'}`);body.appendChild(media);const meta=document.createElement('div');meta.className='svara-modal-media-meta';meta.innerHTML=`<strong>${escapeHtml(filename||'Generation')}</strong><span>${escapeHtml([item.voiceName,item.format,formatBytes(item.sizeBytes)].filter(Boolean).join(' · '))}</span>`;body.appendChild(meta);root.hidden=false;requestAnimationFrame(()=>root.classList.add('is-open'));keyHandler=e=>{if(e.key==='Escape'){e.preventDefault();finish(null);}};document.addEventListener('keydown',keyHandler,true);setTimeout(()=>media.focus(),30);return new Promise(resolve=>{resolver=resolve;});}
  function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}function formatBytes(bytes){const value=Number(bytes)||0;if(!value)return '';if(value<1024)return `${value} B`;if(value<1048576)return `${(value/1024).toFixed(1)} KB`;return `${(value/1048576).toFixed(1)} MB`;}
  window.SvaraModal={rename:openRename,preview:openPreview};
  document.addEventListener('click',event=>{const filename=event.target.closest('.my-library-name');if(filename){const row=filename.closest('.my-library-row');previewItem={filename:filename.querySelector('strong')?.textContent?.trim()||'',row};return;}const button=event.target.closest('.my-library-file-menu button');if(!button)return;const label=button.querySelector('span:last-child')?.textContent?.trim();if(label!=='Preview'||!previewItem?.filename)return;if(!window.SvaraModal?.preview)return;const item={filename:previewItem.filename};const voice=previewItem.row?.children?.[1]?.textContent?.trim();const format=previewItem.row?.children?.[3]?.textContent?.trim();const size=previewItem.row?.children?.[4]?.textContent?.trim();if(voice)item.voiceName=voice;if(format)item.format=format;if(size)item.sizeBytes=size;window.SvaraModal.preview(`/api/generations/media?filename=${encodeURIComponent(item.filename)}`,item);},true);
})();